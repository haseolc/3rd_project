apiVersion: v1
kind: Service
metadata:
  name: ci-cd-smoke-service
  namespace: ci-cd-test
spec:
  type: ClusterIP
  selector:
    app: ci-cd-smoke-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
